# Secrets — never commit
.env
.env.*
*.env

# Python
.venv/
__pycache__/
*.pyc

# Database
company.db
company.db-wal
company.db-shm

# Node
node_modules/

# Cinematic recorder output (PNG frame dumps + encoded .mp4 cutscenes)
recordings/

# OS
.DS_Store
company_role_policy.json

# Per-company workspaces (dbs + per-company data, not source)
workspaces/

# Pipecat Context Hub — cloned MCP server (not our source); built index lives in ~/.pipecat-context-hub
pipecat-context-hub/
